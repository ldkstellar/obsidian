1. 환경설정하기

```bash
source /opt/ros.humble/setup.zsh
```

2. 새로운 디렉토리 생성하기

``` bash
mkdir -p ~/ros2_ws/src
cd ~/ros2_ws/src
```

3. 의존성 해결하기

```bash
cd ..
rosdep install -i --from-path src --rosdistro humble -y
```