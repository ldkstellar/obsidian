라이브러리는 정적(.a, .lib)과 동적(.so, .dll)이 있는데, **삭제 시 실행에 미치는 영향이 다름**.

**🔹 정적 라이브러리 (.a, .lib) 삭제 시**

• 정적 라이브러리는 컴파일 시 실행 파일에 **완전히 포함됨**.

• 따라서, **삭제되어도 실행에는 영향이 없음**.

**🔹 동적 라이브러리 (.so, .dll) 삭제 시**

• 동적 라이브러리는 **실행 시 링크됨**.

• 따라서, **삭제되면 실행이 불가능함**.