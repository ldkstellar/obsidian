기본적으로 복사한 시점에서 소스코드의 스냅샷을 만든다. 코드를 변경하면 빌드를 다시해야한다. 이미지는 모두 읽기 전용이기 때문이다.

##### 이미지 레이어 이해하기
도커는 기본적으로 캐싱으로 변경되지않은 부분은 캐싱하고 변경된부분을 사용하여 새로운 이미지를 생성한다.
레이어 기반 아키텍쳐라고 부른다.

Read only 이미지 위에 컨테이너 레이어(read-write)가 있다.이는 전체 컨테이너의 구조 이다.
즉, 다시 빌드하면 변경되지않는 부분은 빌드하지 않고 변경된 부분만 빌드하여 빌드속도를 높여준다.

