##### 함수를 호출할 때

- 함수는 메모리 안에 "할당"되어 있음
- 함수를 호출하기 위해 필요한 단계
1. 변수들을 스택에 푸쉬(push)
2. 함수 주소로 점프
3. 함수를 실행
4. 호출자 함수로 다시 점프
5. 1번 단계에서 넣어뒀던 변수들을 pop(스택포인터가 위치가 이동하는 것이다.)

- 그래서 좀 더 느림
- cpu 캐시에 최적이 아닐 수도
- 모던 cpu 아키텍처에서는 느림
- 그런데 아직도 이런 잘못된 조언이 떠돌아 다닌다.

![[Pasted image 20250421102025.png]] 

![[Pasted image 20250421102046.png]]
![[Pasted image 20250421102128.png]]

매크로는 사전 처리기에서 # define을 바꿔준다.즉 텍스트를 변경해 준다는거라고 보면 된다.

인라인 함수는 컴파일 도중에 처리한다. # define과 비슷하게

##### 인라인 함수를 쓸때 주의점
- inline은 키워드 힌트일뿐
	- 실제로는 인라인이 안될수 있음
	- 컴파일러는 자기맘대로 아무 함수나 인라인 할수 있다.
- 인라인 함수 구현이 헤더 파일에 위치해야함
	- 복붙을 하려면 컴파일러가 그 구현체를 볼수 있어야 하기에...
	- 각 cpp 파일은 따로 컴파일 됨
	- 따라서 b.h를 인클루드 하는 a.cpp파일을 컴파일 할때 ,컴파일러는 b.cpp에 뭐가 들어있는지 모름
	