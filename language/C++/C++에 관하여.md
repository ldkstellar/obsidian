###  c , c++ 정적타입언어이다.

	하지만 c++은 런타임에서 타입 확인이 가능하다.
	any타입이라는것이 있기 때문이다.

### typeinfo

	typeid는 개체의 런타임 객체나 타입의 개체를 반환한다.

typetrait사용한다.
	컴파일타임에서 isame 템플릿 사용한다.

### Null을 표기 하는 방법

	문자열의 끝을 표기하는 방법 '/0'(null character)
	포인터에 값이 아무것도 없다는 것을 표현하는 방법 NULL 널포인터	
				