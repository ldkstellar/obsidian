- 네임스페이스 그룹
- 현재 쓰레드에 적용되는 도우미 함수들이 있음
	- get_id()
	- sleep_for()
	- sleep_untill()
	- yield()

### yield와 sleep의차이
- sleep는 스레드를 일시 정지 상태를 바꾸는 것이다.반면 yield는 스레드를 실행 대기 상태로 바꾸는 것이다. 결과적으러 스레드를 대기 상태로 바꾸는 것이 더 빠르다.