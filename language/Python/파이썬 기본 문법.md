##### 나눗셈

파이썬3은 정수나눗셈을 할 경우 float로 형변환이 된다.
// 소수점 이하는 버림
% 나머지를 구하는 연산자
** 거듭제곱을 구하는 연산자
##### print 줄바꿈 x
```python
print("he" end="")
print("llo")
```
##### 형변환

```python
int(3.3) # 이런 형태로 형변환 한다.
```

###### 객체의 자료형 알아내기

``` python
type(10) # 숫자형
```

###### 메모리값 알기

```python
id(1) #개체의 고유한 메모리 값이다.
```

##### for 문

```python
for in range(0,11) # 10번 증가

for in range(11,0,-1) # 10번 감소
```

### ㄹ