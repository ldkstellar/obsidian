key값으로 찾는거 매핑

- 키와 값의 쌍들을 저장
- 키는 중복될 수 없음
- c++ 맵은 자동 정렬되는 컨테이너 키기준
- 이진 탐색트리 기반
	- 오름차순
##### std::pair<key,value>
- 두 데이터를 한 단위로 저장하는 구조

##### insert()
- **새요소**를 map에 삽입한다
- 반복자와 bool값을 한쌍으로 반환
	-  반복자는 요소를 가리키고
	-  bool 값은 삽입 결과를 알려줌
- 키를 중복으로 삽입할 수 없음
