1. 프로그램이 메모리에 상주할때 효율적으로 읽고 쓰기 위해서
### 메모리의 구조
1. stack:  밑에서 부터 쌓이고 위에서 해제된다. 맨밑에 main 함수 그리고 위에 다른 함수들이 쌓인다.
2. heap: 정리되지 않은데이터
3. codesection: 이진코드들을 저장하는 공간 고정 되지 않는다. 함수저장 파일형식이다른이유 확장명에 따른 메모리최적화 구조를 다르게 (ex.bin 은 )