개체 지향 프로그램과 관련이 있다.

### 원시 타입
 int형 4byte이면 32bit 그중 1bit sign 타입이다.

### List 
Array 
Linked List 
 1.  저장되면 요소들의 공간
 2. 최대 용량
 3. 크기
메소드의 종류
- add(ele) / apeend(ele)
- add(insert,ele) insert이후의 인덱스의 값을 1씩 쉬프트 해서 이동한다음 삽입 초기에 공간을 많이 잡는다. 
- remove(인덱스) 요소 삭제
- set(index,ele) replace 인덱스
- get(index):인덱스를 알고 싶을 때 사용한다.
- search(key)/contains(key) 찾는것이다.
- sort()/arrange


 