su : root 계정으로 변경
.bashrc 사용자의 쉘개인 초기화 스크립트,시작 할때마다 자동 실행됨.

root계정의 .bashrc와 개인계정의 .bashrc는 다르다. 계정이 바뀌면 해당 초기화 스크립트 실행이됨

윈도우만 20년 쓴 나에게 갑자기 리눅스를 사용하라고?

```bash
KENEL=kenel8 # 변수 할당
echo $KENEL # 변수 호출
echo hello #그냥 호출
sudo passwd root # 패스워드 재설정
```