##### 전처리기가 하는일
프로그래밍에서 코드가 컴파일되기 전에 실행되는 초기 처리 단계

1. #include
2. #ifdef
3.  주석제거
4.  기타 지시자 처리 #pragma 같은 것들 

gcc에 -D옵션은 define을 하는 옵션이다.
makefile은 gcc 옵션들을 모아 하나의 명령어로 만든다.
-wall 은 경고 다작용
-std=gnu11 컴파일러도 버전이 있다 버전 11을 따라서 써라
-save-temps=obj  코드 전처리 과정이 끝난 소스코드를 확인이가능하다