
Soc(칩셋)에 따라 스크립트 문서 옵션을 달리 한다.
##### config 설정하기
make로 아키텍쳐(cpu)에 따른 config파일을 만든다.

``` bash

make bcm2711_defconfig # 칩셋

```


경로는 /project/linuxSrc/linux/arch/arm64/configs

각 cpu 아키텍쳐마다 참조 되어있다.
즉, soc에 맞게끔 설정은 바꿔준다. config에 저장이 된다.

chmod 755 비트욘선