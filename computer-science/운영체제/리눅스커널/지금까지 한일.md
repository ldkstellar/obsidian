1. 라즈베리 파이 구입
2. sd-card 이미지를 구워서 넣었음
3. uboot 부트로더. 커널 이미지
4.  -wif 설정 . -ssh설정
5. apt -get install  필요한 유틸리티를 설치
6. git을 사용해 소스코드를 다운 받았음.
7. bcm2711_defconfig라는 파일에 의해 특성 칩의 커널을 소스를 빌드한다.mcu 종속된 옵션을
8. 컴파일하면 .i .o .s 파일들이 생긴다. 그 결과 imae파일로 만든다.
9. 덮어 씌운다. 

### 라즈베리파이 부팅과정
Rom을 뒤진다. 그다음에 만나는 코드가 uboot로더 코드이다. 
### make menuconifg
 이것을 사용해 쉽게 config 파일을 수정 할 수 있는 것이다.

### set_ftrace에 관하여
커널을 디버깅하는 툴
printf를 찍는것이다.그러나 화면에 찍는 것이 아니라
```bash
/sys/kernel/debug/tracing # 여기에 찍는다.
```

커널의 어떤 속도 저하나 이런것를 최소한으로 하면서 로그를 찍어낸다.

사용 방법
1.ftrace를 활성화하여 컴파일 즉, ftrace_printk 함수를 사용해야 된다는 말이당.
- 