cpu에서 api 호출(cuda vulkan opengl)
드라이버가 api호출을 받아서 GPU 하드웨어가 이해할 수 있는 **저수준 명령어(커맨드 버퍼)**로 변환합니다. 커멘드 버퍼는 요청할 명령을 모아두는 모음집같은것이다. 한꺼번에 실행을 한다.

즉, 드라이버는 단순히 “읽을 수 있게 바꿔주는 것” 이상의 역할을 합니다.
- **API 호출 → 명령 변환 → 메모리 관리 → 실행 스케줄링**까지 처리합니다.